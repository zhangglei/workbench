# 知识库与 Obsidian 同步使用流程

本文档总结当前工作台知识库（`knowledge.js`）与桌面端 Obsidian 同步功能的使用方法、同步流程和注意事项，方便后续维护与实际使用。

---

## 1. 功能目标

当前知识库同步能力支持以下几类数据源协同：

- 浏览器本地 `localStorage`
- 云端知识库接口
  - `/api/knowledge-state`
  - `/.netlify/functions/knowledge-state`
- 桌面端 Obsidian Vault

系统会尽量把三端数据进行合并，而不是简单覆盖，优先保留更新时间较新的内容。

---

## 2. 使用前提

### 2.1 纯网页模式

如果只是在浏览器中打开工作台：

- 可以正常使用知识库的新增、编辑、删除、搜索、标签筛选功能
- 数据会保存在本地浏览器 `localStorage`
- 同时会尝试同步到云端接口
- **不会启用 Obsidian 同步**

### 2.2 桌面端模式（Electron）

如果通过桌面端运行，并且 `window.workbenchApi` 可用：

- 可以读取桌面配置
- 可以设置 Obsidian Vault 路径
- 可以把知识库内容写入 Obsidian
- 可以监听 Obsidian 文件变化并自动回写到知识库页面

---

## 3. 配置流程

### 3.1 打开设置面板

管理员登录后：

1. 打开工作台
2. 点击右上角“设置”
3. 在桌面环境下会看到：
   - 数据目录设置
   - Obsidian Vault 路径设置
   - Obsidian 同步目录设置

### 3.2 配置 Obsidian 路径

推荐流程：

1. 点击 **选择 Obsidian Vault 路径**
2. 选择你的 Obsidian 仓库根目录
3. 设置同步目录名称，例如：`WorkbenchSync`
4. 点击“应用设置”保存

保存后，桌面端会把配置写入本地桌面配置文件，供后续读写 Obsidian 使用。

---

## 4. 知识库日常使用流程

### 4.1 新建笔记

1. 进入“知识库”页面
2. 点击“新建笔记”
3. 填写：
   - 标题
   - 分类
   - 标签
   - 摘要
   - 正文内容
   - 作者
   - 是否置顶
4. 点击保存

保存后会执行：

- 写入当前页面内存状态
- 写入浏览器 `localStorage`
- 推送到云端知识库接口
- 若桌面能力可用，则写入 Obsidian
- 自动记录 `updatedAt`

### 4.2 编辑笔记

1. 在知识库列表或详情页点击“编辑”
2. 修改内容后保存

保存后的同步行为与“新建笔记”一致，并且会刷新 `updatedAt`，用于后续冲突合并。

### 4.3 删除笔记

1. 在列表页点击删除按钮
2. 二次确认后删除

删除后会：

- 从当前状态中移除笔记
- 写入本地存储
- 推送云端

> 注意：当前实现重点是“状态同步”。如果桌面端 Obsidian 侧需要同步删除实体 Markdown 文件，建议后续再单独补充删除策略与安全确认机制。

### 4.4 查看笔记

1. 点击知识库卡片
2. 进入详情页
3. 自动渲染 Markdown

查看时会给笔记阅读量 `views + 1`，并同步保存；当前实现也会刷新 `updatedAt`，这样三端能识别这条记录发生了变化。

---

## 5. 页面初始化同步流程

当进入知识库页面并执行 `KnowledgeBase.init()` 时，整体流程如下：

### 第一步：先加载本地数据

先从 `localStorage` 读取笔记并渲染页面，目的是：

- 避免页面白屏
- 即使离线也能先看到已有数据

### 第二步：再发起远端同步

`syncFromServer()` 会进一步读取：

- 云端接口数据
- Obsidian 数据（如果桌面端能力存在）

### 第三步：按新鲜度合并

系统通过以下字段判断哪条记录更新：

1. `updatedAt`
2. `date`

最终通过 `mergeNotesByFreshness()` 合并，规则为：

- 同一 `id` 的笔记只保留最新版本
- 多来源笔记会合并成一份统一列表
- 合并后会重新写回本地
- 随后再反推送到云端和 Obsidian，尽量保持一致

---

## 6. Obsidian 实时监听流程

初始化时会调用：

- `bindObsidianSync()`

它会在桌面端注册 `window.workbenchApi.onObsidianChanged(...)`。

### 当 Obsidian 文件发生变化时

流程如下：

1. 桌面端检测到同步目录中的笔记变化
2. 将变更后的笔记列表通过事件回传给页面
3. 页面把：
   - 当前内存中的 `state.notes`
   - Obsidian 传回的 `payload.notes`
   进行合并
4. 合并后：
   - 写回本地存储
   - 重新渲染标签和列表
   - 如果当前正在查看某篇笔记，则刷新详情页
   - 弹出“已同步 Obsidian 笔记变更”提示
   - 再把结果推送到云端

这样就能形成“Obsidian 改动 → 页面自动更新”的闭环。

---

## 7. 当前同步优先级说明

当前策略不是固定“本地优先”或“云端优先”，而是：

- **按记录级别的更新时间优先**

也就是说：

- A 笔记本地更新过，就保留本地版本
- B 笔记在 Obsidian 更新过，就保留 Obsidian 版本
- C 笔记只有云端有，就补进本地列表

这是比整库覆盖更安全的方案。

---

## 8. 推荐使用方式

建议按下面流程使用：

1. 管理员先在桌面端配置好 Obsidian Vault 路径
2. 日常主要在工作台知识库页面维护内容
3. 若需要脱离工作台整理文档，可直接在 Obsidian 中修改同步目录下的笔记
4. 切回工作台后，系统会自动监听并同步变更
5. 多设备使用时，云端接口作为额外同步层，降低单端丢失风险

---

## 9. 注意事项

### 9.1 `updatedAt` 很重要

同步合并依赖 `updatedAt`。当前已在以下场景自动更新：

- 新建笔记
- 编辑笔记
- 打开详情页导致阅读量变化

如果后续新增新的写操作，也应同步更新 `updatedAt`。

### 9.2 删除策略需要谨慎

当前删除主要是状态层删除。

如果未来要支持“删除工作台笔记时同步删除 Obsidian Markdown 文件”，建议增加：

- 回收站机制
- 二次确认
- 仅删除同步目录下受控文件
- 防止误删用户手工维护文档

### 9.3 浏览器模式下不会监听 Obsidian

因为浏览器没有 `window.workbenchApi`，所以：

- 不会读写 Obsidian
- 不会绑定 `onObsidianChanged`

这是预期行为。

---

## 10. 适合后续继续增强的方向

后续可以继续增强的点包括：

1. Obsidian 删除同步策略
2. 笔记冲突提示界面
3. 同步状态指示器（本地/云端/Obsidian 是否成功）
4. 手动“立即同步”按钮
5. 为 Obsidian 笔记生成稳定文件名规则
6. 增加同步日志页面，便于排障

---

## 11. 一句话总结

当前知识库同步机制已经形成以下闭环：

**知识库页面编辑 → 本地保存 → 云端同步 → Obsidian 写入 → Obsidian 变更回流 → 页面自动合并刷新。**

它的核心原则是：**多端合并、按更新时间保留最新版本、尽量避免简单覆盖。**
